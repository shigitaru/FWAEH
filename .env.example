# Database
POSTGRES_CONNECTION_STRING=

# SMTP email delivery for local development
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
SMTP_USE_TLS=1

# Disable outgoing emails on hosting if needed
EMAIL_DELIVERY_DISABLED=0

# Optional Resend email API
RESEND_API_KEY=
RESEND_FROM=

# App settings
PICKUP_ADDRESS=Москва, ул. Ленина, 1 (пункт выдачи Protocol Archive)
DEMO_MODE=0

# Supabase Storage
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_BUCKET=media
SUPABASE_PUBLIC_BASE_URL=
